// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
const firebaseConfig = {
  apiKey: "AIzaSyBqvtyv9J3a_l4-y1HrZ8MCZrM5Z2x3HIw",
  authDomain: "verkkokauppa-test.firebaseapp.com",
  projectId: "verkkokauppa-test",
  storageBucket: "verkkokauppa-test.appspot.com",
  messagingSenderId: "524719392355",
  appId: "1:524719392355:web:3ab66c7002dbe758a21a56"
};